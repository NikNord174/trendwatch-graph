# Copy to .streamlit/secrets.toml to enable generated answers in the Chat tab.
# Without a key the tab still works in retrieval-only mode.
OPENAI_API_KEY = "your-key-here"

# Optional model override:
# CHAT_MODEL = "gpt-5-mini"
